{
  "name": "cashflow-dashboard-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Test tooling for the cashflow dashboard. Deliberately NOT in the repo root: Netlify runs `npm install` whenever it finds a package.json in the base directory, and the site itself has no build step and no runtime dependencies.",
  "scripts": {
    "serve": "node server.js",
    "test": "node url-test.js && node rowdetect-test.js",
    "test:unit": "node url-test.js && node rowdetect-test.js",
    "test:e2e": "node e2e.js",
    "test:e2e:nochart": "node e2e.js --no-chart",
    "test:degraded": "node degraded.js",
    "test:connect": "node connect.js",
    "test:tabs": "node tabs.js"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
